---
jail_restart:
  fields:
    entity_id:
      example: binary_sensor.jail_name

jail_start:
  fields:
    entity_id:
      example: binary_sensor.jail_name

jail_stop:
  fields:
    entity_id:
      example: binary_sensor.jail_name

vm_restart:
  fields:
    entity_id:
      example: binary_sensor.virtural_machine_name

vm_start:
  fields:
    entity_id:
      example: binary_sensor.virtural_machine_name
    overcommit:
      example: false

vm_stop:
  fields:
    entity_id:
      example: "binary_sensor.virtural_machine_name"
    force:
      example: false
